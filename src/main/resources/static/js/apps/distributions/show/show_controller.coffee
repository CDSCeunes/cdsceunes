define [ 'cs!app' ], (CDSCeunes) ->
  CDSCeunes.module 'DistributionsApp.ShowController', (Show, CDSCeunes, Backbone, Marionette, $, _) ->
    Show.Controller = showController: (id) ->
    return
  return
